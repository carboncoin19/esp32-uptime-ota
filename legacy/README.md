# Legacy / reference only

Nothing in this folder is built, tested, or maintained.

- `arduino_ide/` — an earlier Arduino IDE sketch of the ESP32-C3 firmware.
  Kept for historical reference only.

The maintained firmware is `src/main.cpp`, built with PlatformIO
(see the repo root README). Do not flash anything from this folder.
